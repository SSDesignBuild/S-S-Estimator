IMPORTANT: This package fixes the GoHighLevel estimate_date_invalid error by removing the server-generated issueDate from the Edge Function payload. GoHighLevel will now apply its own current issue date, matching the manual New Estimate behavior.

After deploying the web app, redeploy the Supabase Edge Function:

npx supabase functions deploy send-to-ghl --use-api --no-verify-jwt

Fixed function version: v24x-ghl-issue-date-default-20260506
