IMPORTANT: This ZIP fixes the frontend and includes the corrected Supabase Edge Function, but Netlify deploys ONLY the web app.

Your current error shows Supabase is still running the old function version:
v24x-ghl-default-terms-no-address-inputs

That old deployed Edge Function does not send the required `title` field to GoHighLevel, so GHL returns:
estimates validation failed: title: Path `title` is required.

After copying this ZIP into your repo, also redeploy the Supabase Edge Function:

npx supabase functions deploy send-to-ghl --use-api --no-verify-jwt

Then test Send to GoHighLevel again.

The corrected function version should show as:
v24x-title-forced-before-ghl-create-20260506
